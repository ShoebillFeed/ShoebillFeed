# Upper bound is load-bearing: mcp 2.x dropped the low-level
# Server.list_tools()/call_tool() decorators this server is built on,
# so an unpinned install resolves to 2.x and crashes on import with
# AttributeError: 'Server' object has no attribute 'list_tools'.
mcp>=1.0.0,<2
httpx>=0.27.0
