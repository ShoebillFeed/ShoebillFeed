# mcp 2.x required: the server uses MCPServer, which replaced the low-level
# Server + @server.list_tools()/@server.call_tool() decorators that 1.x used
# and 2.x removed. Schemas now derive from each tool function's signature.
mcp>=2,<3
httpx>=0.27.0
