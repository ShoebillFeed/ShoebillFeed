fastapi==0.115.12
uvicorn[standard]==0.34.3
pydantic==2.11.4
pydantic-settings==2.9.1
httpx==0.28.1
piper-tts==1.6.0
# torch is intentionally not pinned here -- the Dockerfile installs a
# CPU-only wheel first (small) so it's not pulled in a second time by
# kokoro's own unconstrained "torch" dependency; see Dockerfile for the
# GPU build.
kokoro==0.9.4
